<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="nav_header_vertical_spacing">8dp</dimen>
    <dimen name="nav_header_height">176dp</dimen>
    <dimen name="app_bar_height">180dp</dimen>
    <dimen name="fab_margin">16dp</dimen>
    <dimen name="text_margin">16dp</dimen>

    <!-- Margin -->
    <!-- Margin of a material cards. -->
    <dimen name="card_layout_margin">8dp</dimen>
    <!-- Margin of most layouts. -->
    <dimen name="layout_margin">10dp</dimen>
    <!-- Margin around some of the items. -->
    <dimen name="margin_around_item">10dp</dimen>
    <!-- Margin around material buttons. -->
    <dimen name="button_margin">5dp</dimen>
    <!-- Margin of material cards in recipe detail. -->
    <dimen name="card_margin_detail">1dp</dimen>

    <!-- Height -->
    <!-- Height of app bar with recipe detail image (collapsing). -->
    <dimen name="app_bar_height_detail">300dp</dimen>
    <!-- Height of random recipe image in MainActivity. -->
    <dimen name="random_recipe_image_height">200dp</dimen>
    <!-- Height of recipe image in recipe list. -->
    <dimen name="recipe_item_image_height">150dp</dimen>

    <!-- Padding -->
    <!-- Padding around drawable in Material Chip. -->
    <dimen name="chip_drawable_padding">5dp</dimen>

    <!-- Corder radius -->
    <!-- Corner radius of material cards. -->
    <dimen name="card_corner_radius">15dp</dimen>
</resources>